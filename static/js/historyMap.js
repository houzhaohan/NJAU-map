// 历史记录相关功能
document.addEventListener('DOMContentLoaded', function() {
    // 如果页面上有地图元素，初始化地图
    if (document.getElementById('map')) {
        mapCore.initMap();
    }
    
    // 历史记录特定的事件监听和功能可以在这里添加
    // 加载历史路线、重新规划历史路线等
});